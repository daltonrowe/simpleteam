<div class="rounded-2xl py-5 px-6 text-sm border hover:bg-yin-800 flex flex-col gap-2">

  <a href="<%= team_statuses_path(team) %>" class="font-bold text-2xl">
    <%= team.name %>
  </a>

  <div class="flex gap-4">
    <a href="<%= team_statuses_path(team) %>">Status Updates</a>

    <% if team.user == Current.user %>
      <a href="<%= edit_team_path(team) %>">Team Settings</a>
    <% end %>
  </div>

</div>
