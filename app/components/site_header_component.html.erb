<header class="flex justify-between py-2 px-5">
  <h1>
    <a href="<%= root_path %>">SimpleHero</a>
  </h1>

  <div>
  <% if Current.user %>
    <%= button_to 'Logout', session_path, method: 'delete' %>
  <% end %>

  <% unless Current.user %>
    <%= link_to 'Sign Up', new_registration_path %>
    <%= link_to 'Log In', new_session_path %>
  <% end %>
  </div>
</header>