<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Simple Hero" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-yin-900 text-white">
    <%= render SiteHeaderComponent.new %>
    
    <main class="container mx-auto px-5 sm:mt-20 md:w-2/3 w-full flex flex-col gap-4">
      <%= yield %>
    </main>

    <div class="fixed bottom-0 left-0 p-4 flex flex-col justify-center w-full">
      <%= render AlertComponent.new(text: flash[:alert]) if flash[:alert] %>
      <%= render AlertComponent.new(text: flash[:notice], level: :notice) if flash[:notice] %>
    </div>
  </body>
</html>
